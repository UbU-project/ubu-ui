/* Generated by scripts/generate-typescript.sh. Do not edit by hand. */

/**
 * Initial API request to bootstrap Phase 1 state.
 */
export interface BootstrapRequest {
  requested_at: Timestamp;
  identity: Identity;
}

/**
 * Initial API response containing a compact universe state.
 */
export interface BootstrapResponse {
  responded_at: Timestamp;
  state: UniverseState;
  errors?: Error[];
}

/**
 * Structured human-complete assessment of the six plan-quality signals.
 */
export interface HumanCompletePlanQuality {
  generated_at: Timestamp;
  plan_ref: ObjectReference;
  feedback_latency: number;
  checkpoint_coverage: "adequate" | "sparse" | "absent";
  affect_margin: number;
  violated_dimensions?: string[];
  failure_pattern:
    | "none"
    | "wrong_estimates"
    | "missing_dependencies"
    | "stale_affect"
    | "interruption"
    | "overload"
    | "changed_objective";
  stretch_pressure: "comfort" | "sustainable_stretch" | "destructive_pressure";
  post_plan_state_delta: "better" | "neutral" | "depleted" | "at_risk";
  revision_suggestions: string[];
}

/**
 * User report that a task or work item has been completed outside automation.
 */
export interface HumanCompleteReport {
  reported_at: Timestamp;
  subject: ObjectReference;
  authority_source: AuthoritySource;
  note?: string;
}

/**
 * API response describing the next suggested action without implementing planning logic.
 */
export interface NextActionResponse {
  responded_at: Timestamp;
  task?: Task;
  explanation?: ExplanationFragment;
}

/**
 * API report summarizing risks identified before action or projection.
 */
export interface RiskReport {
  generated_at: Timestamp;
  level: "low" | "medium" | "high";
  findings: {
    category:
      | "deadline_risk"
      | "dependency_fragility"
      | "worker_bottleneck"
      | "stale_affect"
      | "affect_margin"
      | "destructive_pressure"
      | "post_plan_depletion"
      | "low_coverage"
      | "skeleton_failure";
    severity: "low" | "medium" | "high";
    blocking: boolean;
    detail: string;
    subject_ref?: ObjectReference;
  }[];
}

/**
 * User-originated action request submitted to an enforcement gate.
 */
export interface UserActionRequest {
  request_id: string;
  submitted_at: Timestamp;
  authority_source: AuthoritySource;
  action: string;
  target?: ObjectReference;
}

/**
 * Manifest for an exported artifact bundle.
 */
export interface ArtifactManifest {
  artifact_id: string;
  created_at: Timestamp;
  items: SourceReference[];
}

/**
 * Register of claims included in an artifact review or publication package.
 */
export interface ClaimRegister {
  claims: {
    claim_id: string;
    text: string;
    evidence_refs?: string[];
  }[];
}

/**
 * Index of evidence references used by an artifact.
 */
export interface EvidenceIndex {
  evidence: {
    evidence_id: string;
    source: SourceReference;
    summary?: string;
  }[];
}

/**
 * Review record for an artifact export package.
 */
export interface ExportReview {
  reviewed_at: Timestamp;
  reviewer: Identity;
  status: "approved" | "needs_changes" | "blocked";
  notes?: string;
}

/**
 * Plan for publishing an artifact bundle.
 */
export interface PublicationPlan {
  planned_at: Timestamp;
  channel: string;
  manifest: ArtifactManifest;
  review?: ExportReview;
}

/**
 * Closed source-of-authority enum. `user` is direct user authority; `user_override` is only for explicit override cases.
 */
export type AuthoritySource = "user" | "user_override" | "delegated" | "automation_worker" | "policy" | "system";

/**
 * Stable human-readable compartment label.
 */
export type CompartmentLabel = string;

/**
 * A non-negative duration expressed in whole seconds, with optional human-readable ISO 8601 text.
 */
export interface Duration {
  seconds: number;
  iso8601?: string;
}

/**
 * Structured error details for Phase 1 API and tooling responses.
 */
export interface Error {
  code: string;
  message: string;
  path?: string;
}

/**
 * Canonical UbU Phase 1 ID prefix registry, prefix-to-object-type mapping, and UUIDv7 suffix grammar.
 */
export type IdRegistry = {
  [k: string]: unknown;
} & {
  prefix: AllowedPrefix;
  object_type: ObjectType;
  id: AnyUbuId;
};

/**
 * Any canonical UbU Phase 1 ID: a known prefix, one underscore delimiter, and a lowercase unhyphenated UUIDv7 suffix.
 */
export type UbUId = (
  | CommonIdRegistryDefsTaskId
  | CommonIdRegistryDefsObjectiveId
  | CommonIdRegistryDefsPlanId
  | CommonIdRegistryDefsLogEntryId
  | CommonIdRegistryDefsExternalReferenceId
  | CommonIdRegistryDefsCompartmentId
  | CommonIdRegistryDefsSnapshotId
  | CommonIdRegistryDefsAutomationWorkerId
  | CommonIdRegistryDefsProjectionPreviewId
  | CommonIdRegistryDefsCalendarId
  | CommonIdRegistryDefsPreferenceId
  | CommonIdRegistryDefsContainerId
  | CommonIdRegistryDefsUniverseStateId
  | CommonIdRegistryDefsIdentityId
  | CommonIdRegistryDefsRelationshipId
  | CommonIdRegistryDefsExternalEventId
) &
  string;
export type CommonIdRegistryDefsTaskId = string;
export type CommonIdRegistryDefsObjectiveId = string;
export type CommonIdRegistryDefsPlanId = string;
export type CommonIdRegistryDefsLogEntryId = string;
export type CommonIdRegistryDefsExternalReferenceId = string;
export type CommonIdRegistryDefsCompartmentId = string;
export type CommonIdRegistryDefsSnapshotId = string;
export type CommonIdRegistryDefsAutomationWorkerId = string;
export type CommonIdRegistryDefsProjectionPreviewId = string;
export type CommonIdRegistryDefsCalendarId = string;
export type CommonIdRegistryDefsPreferenceId = string;
export type CommonIdRegistryDefsContainerId = string;
export type CommonIdRegistryDefsUniverseStateId = string;
export type CommonIdRegistryDefsIdentityId = string;
export type CommonIdRegistryDefsRelationshipId = string;
export type CommonIdRegistryDefsExternalEventId = string;

/**
 * A monetary amount represented in minor units for a specified ISO 4217 currency.
 */
export interface Money {
  currency: string;
  amount_minor: number;
}

/**
 * Typed reference to another UbU object.
 */
export interface ObjectReference {
  id: UbUId;
  object_type:
    | "Task"
    | "Objective"
    | "Plan"
    | "LogEntry"
    | "ExternalReference"
    | "Compartment"
    | "Snapshot"
    | "AutomationWorker"
    | "ProjectionPreview"
    | "Calendar"
    | "Preference"
    | "Container"
    | "UniverseState"
    | "Identity"
    | "Relationship"
    | "ExternalEvent";
}

/**
 * Compact Legitimizer output summary for an enforcement gate.
 */
export interface PolicySummary {
  legitimization: "accepted" | "needs_review" | "rejected";
  adjudication_reasons?: string[];
  local_only?: boolean;
  no_cloud_llm?: boolean;
  no_external_export?: boolean;
  checked_at: Timestamp;
}

/**
 * Creation, authority path, and source-path metadata for a schema object.
 */
export type Provenance = {
  [k: string]: unknown;
} & {
  created_at: Timestamp;
  created_by?: string;
  authority_source: AuthoritySource;
  source?: SourceReference;
  /**
   * @minItems 1
   */
  source_refs?: [SourceReference, ...SourceReference[]];
};

/**
 * Reference to an external or imported source that informed a UbU object.
 */
export interface SourceReference {
  source_kind: string;
  source_id: string;
  url?: string;
}

/**
 * RFC 3339 timestamp with an explicit timezone designator or offset. Naive timestamps are invalid.
 */
export type Timestamp = string;

/**
 * A registered automation worker that may submit work under delegated or automation authority.
 */
export interface AutomationWorker {
  id: UbUId;
  name: string;
  capabilities: string[];
  registered_at: Timestamp;
}

/**
 * A compartment used to group or scope Phase 1 objects.
 */
export interface Compartment {
  id: UbUId;
  label: CompartmentLabel;
  description?: string;
}

/**
 * A named grouping of UbU object references.
 */
export interface Container {
  id: UbUId;
  name: string;
  items: ObjectReference[];
}

/**
 * An event observed from an external source and linked to a source reference.
 */
export interface ExternalEvent {
  id: UbUId;
  source: SourceReference;
  event_type: string;
  occurred_at: Timestamp;
  summary?: string;
}

/**
 * A canonical reference from UbU to an external system record.
 */
export interface ExternalReference {
  id: UbUId;
  source: SourceReference;
  title: string;
  observed_at: Timestamp;
  provenance: Provenance;
}

/**
 * A stable actor identity used for attribution, without prescribing an authentication system.
 */
export interface Identity {
  id: UbUId;
  subject_key: string;
  display_name?: string;
  kind: "person" | "service" | "automation_worker" | "system";
}

/**
 * Append-only narrative or audit entry linked to a UbU object.
 */
export type LogEntry = {
  [k: string]: unknown;
} & {
  id: UbUId;
  subject?: ObjectReference;
  message?: string;
  event_type?: "compartment_boundary_decided";
  payload?: {
    [k: string]: unknown;
  };
  logged_at: Timestamp;
  authority_source: AuthoritySource;
};

/**
 * Closed reason code explaining why a persisted task lifecycle status is moot.
 */
export type MootReasonCode =
  | "externally_satisfied"
  | "superseded"
  | "delegated"
  | "no_longer_relevant"
  | "invalidated_by_universe_change"
  | "replaced_by_new_plan_structure"
  | "user_declared_moot"
  | "automation_obsolete"
  | "duplicate";

/**
 * A desired outcome that can be decomposed into tasks or plans.
 */
export interface Objective {
  id: UbUId;
  title: string;
  description?: string;
  status: "open" | "active" | "satisfied" | "abandoned";
  priority?: number;
  compartment_id?: UbUId;
  provenance: Provenance;
}

/**
 * A user or system preference that can influence planning without becoming runtime logic.
 */
export interface Preference {
  id: UbUId;
  name: string;
  value: string | number | boolean;
  authority_source: AuthoritySource;
}

/**
 * A typed edge between two UbU object references.
 */
export interface Relationship {
  id: UbUId;
  from: ObjectReference;
  to: ObjectReference;
  kind: string;
}

/**
 * A named point-in-time capture of references, affect observations, and summary metadata.
 */
export interface Snapshot {
  id: UbUId;
  captured_at: Timestamp;
  objects: ObjectReference[];
  affect?: {
    source_kind: "live_observation" | "bootstrap_default_profile";
    observed_at: Timestamp;
    dimensions: {
      energy: AffectDimensionObservation;
      stress: AffectDimensionObservation;
      mood_intensity: AffectDimensionObservation;
    };
  };
  summary?: string;
}

/**
 * Canonical persisted Phase 1 lifecycle status for a task. Readiness and execution states are derived views.
 */
export type TaskStatus = "active" | "completed" | "failed" | "moot";

/**
 * A Phase 1 unit of work with canonical lifecycle status, objective linkage, authority, and provenance.
 */
export type Task = {
  [k: string]: unknown;
} & {
  id: UbUId;
  title: string;
  description?: string;
  status: TaskStatus;
  moot_reason_code?: MootReasonCode;
  objective_id?: UbUId;
  assignee?: Identity;
  blocked_by?: UbUId[];
  due_at?: Timestamp;
  /**
   * Optional duration model. When absent, downstream planning applies its fixed default.
   */
  duration_estimate?:
    | {
        type: "fixed";
        seconds: number;
      }
    | {
        type: "shifted_lognormal_p95";
        min_seconds: number;
        mode_seconds: number;
        p95_seconds: number;
      };
  /**
   * Optional unsigned correlation memberships. When absent, the Task is independent; group values must be unique within this array.
   */
  correlation_groups?: {
    group: string;
    strength: number;
  }[];
  provenance: Provenance;
};

/**
 * Canonical Phase 1 UniverseState facts object for planning or API exchange. This is not a snapshot-view placeholder.
 */
export interface UniverseState {
  id: UbUId;
  captured_at: Timestamp;
  objectives: Objective[];
  tasks: Task[];
}

/**
 * A lightweight view of work that can point to a task, objective, or external reference.
 */
export interface WorkItem {
  ref: ObjectReference;
  summary: string;
  status?: string;
}

/**
 * Imported GitHub CI or workflow run event.
 */
export interface GitHubCIEventSource {
  repository: GitHubRepositorySource;
  run_id: number;
  status: string;
  conclusion?: string | null;
  html_url: string;
  updated_at: Timestamp;
}

/**
 * Imported GitHub issue or pull request comment metadata.
 */
export interface GitHubCommentSource {
  repository: GitHubRepositorySource;
  comment_id: number;
  body: string;
  html_url: string;
  updated_at: Timestamp;
}

/**
 * A batch of GitHub source records imported together.
 */
export interface GitHubImportBatch {
  batch_id: string;
  imported_at: Timestamp;
  issues?: GitHubIssueSource[];
  pull_requests?: GitHubPRSource[];
}

/**
 * Imported GitHub issue source data used to create or update an external reference.
 */
export interface GitHubIssueSource {
  repository: GitHubRepositorySource;
  number: number;
  title: string;
  state: "open" | "closed";
  html_url: string;
  updated_at: Timestamp;
  labels?: GitHubLabelSource[];
}

/**
 * Imported GitHub label metadata.
 */
export interface GitHubLabelSource {
  name: string;
  color: string;
  description?: string | null;
}

/**
 * Imported GitHub milestone metadata.
 */
export interface GitHubMilestoneSource {
  repository: GitHubRepositorySource;
  number: number;
  title: string;
  state: "open" | "closed";
  due_on?: Timestamp;
}

/**
 * Imported GitHub pull request source data.
 */
export interface GitHubPRSource {
  repository: GitHubRepositorySource;
  number: number;
  title: string;
  state: "open" | "closed" | "merged";
  html_url: string;
  updated_at: Timestamp;
}

/**
 * Reference metadata for a GitHub repository imported into UbU.
 */
export interface GitHubRepositorySource {
  owner: string;
  name: string;
  html_url: string;
  default_branch?: string;
}

/**
 * Imported GitHub pull request review metadata.
 */
export interface GitHubReviewSource {
  pull_request: GitHubPRSource;
  review_id: number;
  state: string;
  submitted_at?: Timestamp;
}

/**
 * Standing affect tolerances used by the planning kernel to evaluate point-in-time affect observations.
 */
export interface AffectProfile {
  dimensions: {
    energy: {
      dimension: "energy";
      direction: "higher_is_better";
      location: number;
      scale: number;
      threshold: number;
      freshness_seconds?: number;
    };
    stress: {
      dimension: "stress";
      direction: "lower_is_better";
      location: number;
      scale: number;
      threshold: number;
      freshness_seconds?: number;
    };
    mood_intensity: {
      dimension: "mood_intensity";
      direction: "lower_is_better";
      location: number;
      scale: number;
      threshold: number;
      freshness_seconds?: number;
    };
  };
}

/**
 * A planning calendar with bounded availability windows.
 */
export interface Calendar {
  id: UbUId;
  timezone: string;
  windows: {
    start: Timestamp;
    end: Timestamp;
  }[];
}

/**
 * Short explanation text attached to a planning object or validation result.
 */
export interface ExplanationFragment {
  subject?: ObjectReference;
  text: string;
  severity: "info" | "warning" | "error";
}

/**
 * One ordered step in a Phase 1 plan.
 */
export interface PlanStep {
  index: number;
  task_id?: UbUId;
  summary: string;
  start?: Timestamp;
  end?: Timestamp;
  static_anchor?: boolean;
  depends_on?: number[];
}

/**
 * A candidate or admitted ordered set of plan steps.
 */
export interface Plan {
  id: UbUId;
  objective_id?: UbUId;
  supersedes_plan_id?: UbUId;
  status: "candidate" | "admitted" | "rejected" | "superseded";
  steps: PlanStep[];
  created_at: Timestamp;
}

/**
 * Requested GitHub issue or pull request comment write payload.
 */
export interface GitHubCommentWrite {
  repository: GitHubRepositorySource;
  issue_number: number;
  body: string;
}

/**
 * Requested GitHub label write payload.
 */
export interface GitHubLabelWrite {
  repository: GitHubRepositorySource;
  issue_number: number;
  labels: string[];
}

/**
 * Requested creation or update of a GitHub issue managed by UbU.
 */
export interface GitHubManagedIssueWrite {
  repository: GitHubRepositorySource;
  issue_number?: number | null;
  title: string;
  body: string;
  labels?: string[];
}

/**
 * Human or delegated approval for a projection preview.
 */
export interface ProjectionApproval {
  preview_id: UbUId;
  approved: boolean;
  approved_at: Timestamp;
  authority_source: AuthoritySource;
  note?: string;
}

/**
 * A proposed projection write operation to an external system.
 */
export interface ProjectionOperation {
  operation_id: string;
  kind: "create" | "update" | "comment" | "label";
  target: SourceReference;
  summary: string;
}

/**
 * A preview of external-system writes before they pass an enforcement gate.
 */
export interface ProjectionPreview {
  id: UbUId;
  created_at: Timestamp;
  operations: ProjectionOperation[];
  policy_summary?: PolicySummary;
}

/**
 * Comparison of expected projection state with observed external state.
 */
export interface ProjectionReconciliation {
  source: SourceReference;
  checked_at: Timestamp;
  status: "matched" | "drifted" | "missing";
  notes?: string;
}

/**
 * Outcome of applying projection operations to an external system.
 */
export interface ProjectionResult {
  preview_id: UbUId;
  applied_at: Timestamp;
  status: "applied" | "partial" | "failed";
  operation_results: {
    operation_id: string;
    status: "applied" | "skipped" | "failed";
    message?: string;
  }[];
}

/**
 * A schema object admitted through an enforcement gate into the store.
 */
export interface AdmittedObject {
  object: ObjectReference;
  admitted_at: Timestamp;
  policy_summary?: PolicySummary;
  provenance: Provenance;
}

/**
 * A proposed object waiting for validation and legitimization.
 */
export interface CandidateObject {
  candidate_id: string;
  object_type: string;
  payload: {
    [k: string]: unknown;
  };
  submitted_at: Timestamp;
  authority_source: AuthoritySource;
}

/**
 * Record of a store migration affecting schema-versioned objects.
 */
export interface MigrationRecord {
  migration_id: string;
  from_version: string;
  to_version: string;
  applied_at: Timestamp;
  summary?: string;
}

/**
 * History metadata for changes observed on a stored object.
 */
export interface ObjectHistory {
  object: ObjectReference;
  events: StoreEvent[];
}

/**
 * A closed-vocabulary trigger for recalculating derived Phase 1 state.
 */
export interface RecalculationTrigger {
  triggered_at: Timestamp;
  trigger_type:
    | "task_completed"
    | "task_failed"
    | "task_moot"
    | "user_override"
    | "observed_snapshot"
    | "external_event"
    | "github_update"
    | "low_compact_calendar_coverage"
    | "worker_request";
  note?: string;
  objects?: ObjectReference[];
}

/**
 * A store-level event describing admission, update, migration, or recalculation.
 */
export interface StoreEvent {
  event_type: "admitted" | "updated" | "recalculated" | "migrated";
  occurred_at: Timestamp;
  object?: ObjectReference;
  summary?: string;
}

/**
 * Request for advisory GPU capacity or scheduling guidance from an automation worker.
 */
export interface GPUAdvisoryRequest {
  request_id: string;
  requested_at: Timestamp;
  workload: string;
  estimated_duration?: Duration;
  max_cost?: Money;
}

/**
 * Advisory GPU capacity response from an automation worker.
 */
export interface GPUAdvisoryResponse {
  request_id: string;
  responded_at: Timestamp;
  recommendation: "run_now" | "defer" | "split" | "manual_review";
  rationale?: string;
  estimated_cost?: Money;
}

/**
 * Authority context attached to an automation worker submission.
 */
export interface WorkerAuthority {
  worker_id: UbUId;
  authority_source: AuthoritySource;
  delegated_by?: Identity;
}

/**
 * Result envelope returned from an automation worker.
 */
export interface WorkerResult {
  submission_id: string;
  completed_at: Timestamp;
  status: "ok" | "failed" | "needs_review";
  errors?: Error[];
}

/**
 * Envelope for an automation worker submitting a candidate result.
 */
export interface WorkerSubmission {
  submission_id: string;
  submitted_at: Timestamp;
  authority: WorkerAuthority;
  payload: {
    [k: string]: unknown;
  };
}
